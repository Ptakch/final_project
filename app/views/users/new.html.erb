<h1> New User </h1>

<%= form_for @user, url: users_path do |u| %>

	<p>
		<%=  u.label :name %>
		<%= u.text_field :name %>
	</p>

	<p>
		<%= u.label :email %>
		<%= u.text_field :email %>
	</p>

	<p>
		<%= u.submit %>
	</p>

	<% end %>